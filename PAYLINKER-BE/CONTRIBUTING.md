# Contributing Guide

본 문서는 백엔드 팀의 Git 컨벤션과 GitHub 기반 워크플로우를 정의합니다.  
**모든 작업은 반드시 아래 순서를 따릅니다.**

---

## 목차

1. [브랜치 전략](#1-브랜치-전략)
2. [작업 흐름](#2-작업-흐름)
3. [이슈 컨벤션](#3-이슈-컨벤션)
4. [브랜치 네이밍](#4-브랜치-네이밍)
5. [커밋 메시지](#5-커밋-메시지)
6. [Pull Request](#6-pull-request)
7. [라벨 규칙](#7-라벨-규칙)
8. [코드 컨벤션](#8-코드-컨벤션)

---

## 1. 브랜치 전략

GitHub Flow를 기본으로 하되, GitFlow의 `dev` / `master` 분리 개념을 부분 적용합니다.

| 브랜치 | 역할 | 직접 커밋 |
|--------|------|-----------|
| `master` | 프로덕션·릴리즈 브랜치 (정기 배포) | ❌ 금지 |
| `dev` | 통합·테스트 브랜치 (무중단 배포, staging 서버) | ❌ 금지 |
| `feat/#n-...` 등 | 개별 작업 브랜치 | ✅ |

> 모든 기능은 `dev`를 거쳐 `master`로 반영됩니다.  
> `master`에는 `dev`를 통한 PR만 허용하며, 릴리즈 단위로 머지합니다.

---

## 2. 작업 흐름

```
Issue 생성 → 브랜치 생성 → 작업 & 커밋 → PR 생성(→ dev) → dev 테스트 → master 머지
```

---

## 3. 이슈 컨벤션

모든 작업은 **Issue 생성**으로 시작합니다.

### 이슈 템플릿

| 템플릿 | 사용 시기 |
|--------|-----------|
| ✨ 기능 (Feature) | 새로운 기능 구현, 기존 기능 개선 |
| 🐛 버그 (Bug) | 버그 발견 및 수정 |
| 🛠️ 태스크 (Task) | 문서, 리팩토링, 설정 변경 등 |

### 이슈 제목 규칙

```
prefix: 작업과 관련된 한국어 설명
```

**예시**
```
feat: 회원가입 API 추가
fix: 저장 시 NPE 발생 문제 수정
docs: swagger 설명 보완
```

### Prefix 목록

| Prefix | 설명 |
|--------|------|
| `feat` | 새로운 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서 작성·수정 |
| `refactor` | 리팩토링 (기능 변경 없음) |
| `test` | 테스트 코드 |
| `chore` | 빌드 설정, 의존성, 기타 |
| `style` | 코드 포맷, 세미콜론 등 |
| `perf` | 성능 개선 |
| `ci` | CI/CD 설정 |

---

## 4. 브랜치 네이밍

```
prefix/#issue번호-description-with-dash-english
```

**예시**
```
feat/#35-add-login
fix/#72-null-pointer-on-save
docs/#88-update-swagger
test/#120-archive
```

- `description`은 **영문 kebab-case** 사용
- prefix는 [Prefix 목록](#prefix-목록)과 동일

---

## 5. 커밋 메시지

```
prefix: 커밋 내용 (한국어)
```

**예시**
```
feat: 회원가입 API 구현
fix: 저장 시 NPE 발생 문제 수정
docs: swagger 설명 추가
refactor: UserService 책임 분리
```

**주의사항**
- 하나의 커밋에는 **하나의 논리적 변경**만 포함
- prefix는 반드시 **소문자** 사용

---

## 6. Pull Request

### PR 대상 브랜치

- 기능 브랜치 → **`dev`** (항상)
- `dev` → `master` (릴리즈 단위)

### PR 제목 형식

```
prefix: 한국어 설명 (#이슈번호)
```

**예시**
```
feat: 회원관리 기능 개발 (#32)
fix: 이메일 발송 실패 처리 (#58)
```

### Assignee / Reviewer

| 항목 | 설정 |
|------|------|
| Assignee | **본인** |
| Reviewer | **타 파트원** |

### PR 체크리스트

머지 전 아래 항목을 모두 확인해주세요.

- [ ] 이슈 번호가 연결되어 있다 (`closes #n`)
- [ ] 브랜치 대상이 `dev`로 설정되어 있다
- [ ] Assignee가 본인으로 설정되어 있다
- [ ] Reviewer가 타 파트원으로 설정되어 있다
- [ ] prefix / status / priority 라벨이 각각 설정되어 있다
- [ ] 코드 컨벤션을 준수하였다
- [ ] 하나의 커밋에 하나의 논리적 변경만 포함되어 있다

---

## 7. 라벨 규칙

이슈와 PR 모두 아래 세 가지 카테고리에서 **각 1개씩 필수로 설정**합니다.

### PREFIX 라벨 (작업 유형)

| 라벨 | 색상 | 설명 |
|------|------|------|
| `feat` | 🟣 | 새로운 기능 구현 |
| `fix` | 🔴 | 버그 수정 |
| `docs` | 🔵 | 문서 작성·수정 |
| `refactor` | 🌸 | 리팩토링 |
| `test` | 🔷 | 테스트 코드 |
| `chore` | 🟡 | 빌드·의존성·기타 |
| `style` | 🟤 | 코드 스타일 |
| `perf` | 🔹 | 성능 개선 |
| `ci` | 🔴 | CI/CD 설정 |

### STATUS 라벨 (진행 상태)

| 라벨 | 설명 |
|------|------|
| `status: todo` | 아직 시작하지 않은 작업 |
| `status: in progress` | 현재 진행 중 |
| `status: in review` | 코드 리뷰 진행 중 |
| `status: blocked` | 블로킹 상태 |
| `status: done` | 작업 완료 |

### PRIORITY 라벨 (우선순위)

| 라벨 | 설명 |
|------|------|
| `priority: high` | 긴급 처리 필요 |
| `priority: medium` | 일반 우선순위 |
| `priority: low` | 여유 있을 때 처리 |

---

## 8. 코드 컨벤션

Java / Spring / Spring Boot 공식 컨벤션을 따릅니다.  
상세 내용은 팀 내부 컨벤션 문서를 참고해주세요.

### 핵심 원칙

- 들여쓰기: **4 spaces** (탭 사용 금지)
- 줄 길이: **최대 120자**
- 클래스명: **PascalCase**
- 메서드·변수명: **camelCase**
- 상수: **UPPER_SNAKE_CASE**
- 패키지명: **소문자**, 단어 구분 없이 연속 작성

### 주석

- 공개 API에는 **Javadoc** 작성
- 복잡한 비즈니스 로직에는 인라인 주석 추가
- 주석은 **한국어**로 작성

### .github 설정 변경

`.github` 폴더 내 설정 변경이 필요한 경우 **@김겨레** 와 사전 논의 후 진행합니다.
